{
  "name": "cobutech-industriy",
  "version": "1.0.0",
  "main": "server/server.js", // Corrected path to your main server file
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js" // Useful for local development
  },
  "dependencies": {
    "bcrypt": "^5.1.0",       // For password hashing
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "firebase-admin": "^11.5.0",
    "jsonwebtoken": "^9.0.0",  // For creating authentication tokens
    "mongoose": "^7.0.3",
    "nodemailer": "^6.9.10"    // For sending emails (verification codes)
  },
  "devDependencies": {
    "nodemon": "^3.0.1"       // For development server with auto-reload
  },
  "engines": {
    "node": ">=18.0.0"         // Specify a compatible Node.js version
  }
}
